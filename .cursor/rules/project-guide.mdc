---
description: RentHub 开发者指南文档站全局规范
alwaysApply: true
---

# RentHub 开发者指南 — 项目规范

## 项目性质

本仓库是 **文档项目**，使用 **Jekyll** 构建静态站点并部署到 GitHub Pages。
目标是为 RentHub 团队提供一站式、快速上手的开发者参考手册。

RentHub 是基于微信小程序的租赁市场平台，采用 Monorepo 架构：
- `miniprogram/` — 微信小程序前端（WXML + WXSS + JS）
- `cloudfunctions/` — 云函数（user / asset / order / help / message / admin）
- `admin/` — React 18 + TypeScript Web 管理后台
- `website/` — 官方网站

---

## 技术栈

- **静态站点生成器**：Jekyll 4.3
- **Markdown 引擎**：kramdown（GFM 模式）
- **语法高亮**：Rouge
- **部署**：GitHub Actions → GitHub Pages
- **主题**：自定义布局 + CSS（主题色 `#3d7c47` / `#7cb342`）

---

## 目录结构规范

```
_config.yml              # Jekyll 配置
Gemfile                  # Ruby 依赖
index.md                 # 站点首页（layout: home）

_layouts/                # Jekyll 布局模板
  default.html           # 基础布局
  home.html              # 首页布局
  doc.html               # 文档页布局

_includes/               # 可复用 HTML 片段
  header.html            # 顶部导航
  footer.html            # 页脚
  sidebar.html           # 文档侧边栏

assets/
  css/style.css          # 全站样式
  js/main.js             # 交互脚本
  images/                # 图片资源

docs/                    # 所有文档内容
  vibe-coding/           # Vibe Coding（AI 辅助开发规范）
  project-mgmt/          # 项目管理（分支策略、发布流程、Code Review）
  overview/              # 项目综述（架构、技术选型、模块关系）
  dev-notes/             # 开发注意事项（环境、调试、常见坑）
  knowledge/             # 开发所需知识（云开发、小程序、TypeScript 等）
  <future-topic>/        # 新板块按需在此层级新增
```

- 每个板块下用 `README.md` 作为入口索引，需在 front matter 中设置 `permalink`
- 子话题用独立 `.md` 文件，文件名使用 **kebab-case**（全小写、连字符分隔）
- 文档内容放在 `docs/` 下；布局、样式、脚本分别放在 `_layouts/`、`_includes/`、`assets/`

---

## 文件命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 板块入口 | `README.md` | `docs/overview/README.md` |
| 普通文档 | `kebab-case.md` | `cloud-functions.md` |
| 板块目录 | `kebab-case/` | `dev-notes/` |

- 名称应简洁且自解释，见名知义
- 避免缩写（除非约定俗成，如 `db`、`api`）

---

## Markdown 文档规范

### Front Matter

每个 `docs/` 下的 `.md` 文件必须包含 YAML front matter：

```yaml
---
title: 页面标题
---
```

板块入口 `README.md` 还需指定 `permalink`：

```yaml
---
title: Vibe Coding 指南
permalink: /docs/vibe-coding/
---
```

子文档无需指定 `permalink`，Jekyll 会根据文件路径自动生成 URL。

### 写作规范

- **语言**：中文为主，技术术语保留英文原词
- **标题层级**：`#` 只用于文档标题（由 `doc.html` 布局从 `page.title` 渲染），正文从 `##` 开始，最深不超过 `####`
- **标题编号**：正文各级标题必须添加数字编号，便于阅读与引用
  - 一级正文标题：`## 1. 概述`、`## 2. 安装`
  - 二级正文标题：`### 1.1 背景`、`### 1.2 目标`
  - `README.md` 入口索引文件不强制编号
- **结构**：每篇文档需包含：概述 → 详细说明 → 示例（如适用）→ 注意事项
- **长度**：单文件建议不超过 500 行；内容过多时拆分子文件并在 README.md 中链接
- **代码块**：必须标注语言，如 ` ```js `、` ```bash `

---

## 文档导航规范

文档之间的跳转必须使用可点击的 Markdown 链接，禁止仅用代码块（反引号）展示路径。

### 链接写法

使用 **Liquid `relative_url` 过滤器**生成链接，确保在任何 `baseurl` 下都能正确跳转：

```markdown
<!-- 正确：使用 Liquid 模板 -->
[← 返回文档首页]({{ '/' | relative_url }})
[Cursor 使用指南]({{ '/docs/vibe-coding/cursor-guide.html' | relative_url }})

<!-- 错误：硬编码相对路径 -->
[← 返回文档首页](../../README.md)
[cursor-guide.md](./cursor-guide.md)
```

### 每篇文档必须包含的导航元素

| 文档类型 | 必须包含 |
|----------|---------|
| `index.md`（首页） | 指向各板块的目录表（含链接与状态列） |
| 板块 `README.md` | 顶部返回首页链接 + 指向子文档的表格（含链接） |
| 子文档（`.md`） | 顶部面包屑导航：返回板块 + 返回首页 |

### 面包屑格式

子文档顶部（front matter 之后第一行）统一使用：

```markdown
[← 返回 <板块名> 板块]({{ '/docs/<板块>/' | relative_url }}) · [← 返回文档首页]({{ '/' | relative_url }})
```

板块 `README.md` 顶部使用：

```markdown
[← 返回文档首页]({{ '/' | relative_url }})
```

### 维护要求

- 新增文档时，**同步**更新：
  1. 父级 `README.md` 的表格中添加链接条目
  2. `_includes/sidebar.html` 中添加侧边栏导航项
  3. `index.md` 首页目录表（如新增板块）
- 重命名或移动文件时，**同步**更新所有引用该文件的链接
- 目录表的"状态"列保持更新（`待撰写` / `进行中` / 留空表示完成）

---

## 样式与布局规范

- **主题色**：`#3d7c47`（primary）、`#7cb342`（primary-light），定义在 `assets/css/style.css` 的 `:root` 变量中
- **布局选择**：首页用 `home`，文档页用 `doc`（通过 `_config.yml` defaults 自动应用）
- **新增布局**：放在 `_layouts/` 下；可复用片段放在 `_includes/` 下
- **样式修改**：统一在 `assets/css/style.css` 中修改，使用 CSS 变量保持一致性

---

## 可扩展性原则

- 新增板块时，在 `docs/` 下新建对应目录，并同步更新：
  1. `index.md` 首页目录表
  2. `_includes/sidebar.html` 侧边栏
  3. `_includes/header.html` 顶部导航（如需要）
- 不修改现有板块的目录名（避免破坏已有链接）
- 公共术语、缩写说明统一维护在 `docs/knowledge/glossary.md`
